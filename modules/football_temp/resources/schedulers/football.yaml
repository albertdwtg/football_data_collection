---
description: Empty
schedule: ${schedule-cron[env]}
paused: ${schedule-paused[env]}
http_target:
  http_method: post
  uri: "http://example.com"
  headers:
    Content-Type: application/json
# oidc_token:
#   service_account_email: "empty@email.com"
